# The URL the site will be built for.
base_url = "https://skyplabs.com"

# The site title and description.
title = "SkypLabs"
description = "Security and Software Engineering, IT Consultancy and Content Creation"

# The default language used in feeds.
default_language = "en"

# The site theme to use.
theme = "deepthought"

# Whether to automatically compile all Sass files in the sass directory.
compile_sass = true

# When set to "true", the generated HTML files are minified.
minify_html = true

# When set to "true", a feed is automatically generated.
generate_feed = true

# Whether to build a search index to be used later on by a JavaScript library.
build_search_index = true

taxonomies = [
    { name = "categories", feed = true, paginate_by = 10 },
    { name = "tags", feed = true, paginate_by = 10 },
]

[markdown]
# When set to "true", all code blocks are highlighted.
highlight_code = true

# When set to "true", emoji aliases translated to their corresponding Unicode
# emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄).
render_emoji = true

# Whether external links are to be opened in a new tab.
external_links_target_blank = true

# Whether to set rel="nofollow" for all external links.
external_links_no_follow = true

# Whether to set rel="noreferrer" for all external links.
external_links_no_referrer = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their
# typographic form).
smart_punctuation = true

[extra]
navbar_items = [
  { code = "en", nav_items = [
    { url = "$BASE_URL/", name = "Home" },
    { url = "$BASE_URL/posts", name = "Posts" },
  ]},
]

[extra.author]
name = "SkypLabs"
avatar = "/images/logo.png"

[extra.company]
legal_name = "SkypLabs Limited"
address = [
  "71 Lower Baggot Street",
  "Ground Floor",
  "D02 P593",
  "Dublin, Ireland",
]
# Used to automatically update the copyright year range.
incorporation_year = "2021"

[extra.favicon]
webmanifest = "/favicon/site.webmanifest"
favicon_16x16 = "/favicon/favicon-16x16.png"
favicon_32x32 = "/favicon/favicon-32x32.png"
apple_touch_icon = "/favicon/apple-touch-icon.png"

[extra.social]
email = "hello@skyplabs.com"
github = "SkypLabs-Ltd"
mastodon_username = "SkypLabs"
mastodon_server = "infosec.exchange"
